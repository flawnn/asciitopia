// Root export surface — the component and its props; nothing re-exported from
// core (consumers import core types from @asciitopia/core directly).

export { AsciiBackground } from './ascii-background.js';
export type { AsciiBackgroundProps } from './ascii-background.js';
